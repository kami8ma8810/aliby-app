{
  "installCommand": "git clone https://github.com/flutter/flutter.git -b stable && export PATH=\"$PATH:`pwd`/flutter/bin\"",
  "buildCommand": "export PATH=\"$PATH:`pwd`/flutter/bin\" && flutter pub get && flutter build web --release --web-renderer html",
  "outputDirectory": "build/web"
}