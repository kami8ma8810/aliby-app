{
  "buildCommand": "cd $HOME && git clone https://github.com/flutter/flutter.git -b stable && export PATH=\"$PATH:$HOME/flutter/bin\" && cd $VERCEL_PATH_0 && flutter --version && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "framework": null
}